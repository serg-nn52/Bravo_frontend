import React from 'react';
import style from './style.module.scss';

const Header: React.FC = () => {
  return <header className={style.header}>Заявки на литературу</header>;
};

export default Header;
